import * as React from 'react';
import {render} from 'react-dom';
import ProductionListContainer from './components/productionList';

render((<ProductionListContainer />), document.getElementById('app'));

